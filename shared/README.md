# shared

TypeScript types and event contracts shared between `client` and `server`, so both use the same data models. Empty placeholder — scaffolded in Milestone 1, see [../docs/roadmap.md](../docs/roadmap.md).

Planned structure (per [../docs/engineering/architecture.md](../docs/engineering/architecture.md)): `types`, `events`, `game-state`.
