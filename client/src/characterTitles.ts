import type { PlayerColorId } from '@custom-tabletop/shared';

/** Who each color plays as — shown next to the color picker. */
export const CHARACTER_TITLES: Record<PlayerColorId, string> = {
  red: 'the Regular',
  blue: 'the Local',
  green: 'the Ranger',
  yellow: 'the Explorer',
  purple: 'the Rebel',
  orange: 'the Rocker',
};
