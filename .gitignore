# Dependencies
node_modules/
.pnp/
.pnp.js

# Build output
dist/
build/
*.tsbuildinfo

# Env / secrets
.env
.env.local
.env.*.local

# Logs
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Editor
.vscode/*
!.vscode/extensions.json
.idea/

# OS
.DS_Store
Thumbs.db

# Test / coverage
coverage/

# User-uploaded files (map images, soundboard audio) — server-local, not source
server/uploads/

# Raw third-party asset packs (~500MB, re-downloadable — see
# blender/source-assets/README.md). Only the processed outputs are tracked:
# blender/*.blend (editable source) and client/public/models/*.glb (runtime).
blender/source-assets/*
!blender/source-assets/README.md

# Blender auto-backups
*.blend1

# Playwright output
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
