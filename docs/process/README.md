# Process

← [CLAUDE.md](../../CLAUDE.md)

How work happens in this repo — not what the app is (see [docs/engineering/](../engineering/README.md)). Just the mechanics of making a change.

| File | Read when... |
|---|---|
| [git-workflow.md](git-workflow.md) | Before any commit, branch, or merge — naming, scope discipline, the merge-back checklist |
| [session-handover.md](session-handover.md) | Ending a session with work in flight, or starting one — the `**Next session** →` handover block |

Also codified as Claude Code skills under [`.claude/skills/`](../../.claude/skills/) (`feature-workflow`, `sanity-check`), so they get followed automatically rather than relying on this doc being re-read from memory each session.

This is a small, solo project. There's deliberately no parallel-subagent workflow doc here — see [decisions.md](../decisions.md) for why. If the project grows enough to need one, add it then rather than pre-building it now.
