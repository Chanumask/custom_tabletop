# Decisions

← [CLAUDE.md](../CLAUDE.md)

Running log of decisions worth remembering across sessions. Newest first. Each entry: what was decided, why, and what it rules out.

---

## 2026-09-24 — Milestone 5: tabletop map & drawing — draw-on-the-physical-table interaction, canvas-texture pipeline, deliberate scope cuts

**Decided**, closing out Milestone 5. Two scope calls were made with the user up front (see the questions asked at the start of this work) before any code was written:

1. **Drawing happens by raycasting mouse input against the table's own mesh while pointer-lock is released — not a flat 2D overlay.** Matches `docs/engineering/architecture.md`'s framing of the map as "a physical surface on the table inside the room," not a floating UI panel. Concretely: releasing pointer lock (Esc, or never locking) shrinks the "click to look around" prompt from the full-screen button it was through Milestone 4 down to a small non-blocking pill at the bottom of the screen (`style.css`), freeing the rest of the viewport — including the rendered table — for direct click-drag interaction (`TableDrawing.ts`). A pointerdown/move/up sequence raycasts against the `Table_Top` mesh; a hit's local X/Z (relative to the table's own center) converts to canvas pixel coordinates (`tableCoordinates.ts`). **Rules out:** a separate 2D drawing-mode panel disconnected from the 3D scene.
2. **The table's canvas texture UV is recomputed in Three.js at runtime (`tableTopUV.ts`), not fixed in Blender.** Blender's default cylinder UV unwrap splits top/side/bottom into separate islands, not one clean 0..1 square — so a canvas texture applied via the mesh's real UV would display cropped/distorted. Recomputing UV from each vertex's local X/Z (mapping the inscribed circle onto the full unit square) sidesteps needing to fix this in Blender at all, and — critically — uses the exact same formula `tableLocalToCanvas` uses for raycast input, so a stroke always lands exactly where the player clicked. `TableCanvas`'s texture sets `flipY = false` so canvas-row-from-top maps directly to UV `v` with no inversion either function has to compensate for. **Verified this mapping is correct in a real browser**, not just via the two functions' matching unit tests: click-dragging on the table produced a stroke exactly under the cursor, and a test background image (a data-URI, to sidestep an external-network/CORS restriction in the sandboxed test browser — see below) filled the table's full circular top edge-to-edge with no crop or distortion.
3. **Scope cut, agreed with the user before implementation: one active scene per session that the host retargets by background-image URL (`scene:update`), not a multi-scene manager.** `scene:create`/`scene:change` are still implemented, validated, and unit-tested server-side (satisfying the roadmap's literal event list), but nothing in the UI calls them yet — only `scene:update`, against the one scene every session is seeded with at creation (`createDefaultScene`, mirroring M2's "implicit creation" pattern applied to scenes). **Rules out:** named multi-scene UI (creation form, switcher) for this milestone; a real follow-up if multi-map support becomes an actual need.
4. **`Drawing.points` changed from the original spec's `Vector3[]` to a new `Point2D` (`shared/src/types.ts`), and `GameState`'s top-level `drawings: Drawing[]` (redundant with `Scene.drawings`, and never consumed by any code before this milestone) was dropped.** A drawing stroke lives entirely on a flat canvas texture — reusing `Vector3` would leave `z` permanently unused, a footgun for a future reader who might assume it means something. Two sources of truth for drawings (top-level `GameState.drawings` *and* `Scene.drawings`, both in the original spec, keyed by the same `sceneId`) was clearly an oversight, not a deliberate design in the handed-over spec — grep confirmed nothing referenced `GameState.drawings` before this session touched it. Logged per `docs/engineering/architecture.md`'s own "Changing this spec" instruction (deviations get logged here and that file updated, not silently diverged).
5. **`player:move`'s pattern (no ack, no full-state broadcast, fire-and-forget, server rebroadcasts the same delta via `socket.to(sessionId)` excluding the sender) is reused as-is for `drawing:start/update/end/delete`.** Same justification as Milestone 4's entry: these can fire many times a second while a player drags a stroke. `scene:create/change/update`, by contrast, are infrequent and host-gated, so they keep `session:join`/`leave`'s pattern instead — a real ack plus a full `session:state` broadcast. Two different sync patterns living side by side in the same event model, chosen per-event by frequency/authority rather than picking one pattern for everything.

**Verified:** `server/src/tabletopMap.test.ts` (5 tests) — the host's `scene:update` is seen live by another player and rejected for a non-host; a drawing stroke is seen live point-by-point by another player and not just at the end; a later joiner's initial `session:state` already contains a fully-drawn stroke; `drawing:delete` is seen live. Break-round done on both halves of the exit check (scene broadcast and drawing broadcast disabled separately, each confirmed to fail the right test, then restored). `client/src/three/tableTopUV.test.ts` and `tableCoordinates.test.ts` cover the coordinate math in isolation (no DOM/canvas needed for that, unlike `TableCanvas`/`TableDrawing` themselves, which touch real `HTMLCanvasElement`/pointer-event APIs unavailable under Vitest's `node` environment and are instead covered by the browser check below — same split as `FirstPersonController`/`collision.ts` since Milestone 3). Full `sanity-check` (lint/format/build/test, 120 tests across all 3 workspaces) clean. **Real two-tab browser verification**, and it caught a real thing worth noting: an attempt to load a live Wikimedia Commons image as a background failed (`fetch(...).ok === false`) — an external-network/CORS restriction of the sandboxed test browser itself, confirmed by testing a `data:` URI generated in-page instead, which loaded and displayed correctly, edge-to-edge, with correct lighting. With that substituted, both scope-cut features were confirmed end-to-end with two real browser tabs: the host's map switch appeared live on the second tab without a refresh, and a non-host player's drawn stroke appeared live on the host's tab, layered correctly on top of the already-set background.

## 2026-09-23 — Milestone 4: player avatars — yaw-only rotation, delta-only `player:move`, membership vs. live-movement split on the client

**Decided**, four related calls implementing Milestone 4.

1. **`Player` gained `rotationY` (yaw only, radians) — no pitch/roll.** A placeholder capsule avatar only needs to face a direction; tracking full camera orientation would be unused data pushed over the wire on every move. Computed from `camera.getWorldDirection()` (`FirstPersonController.getYaw()`), not read off `camera.rotation.y` directly — `PointerLockControls` manages pitch/yaw internally via a `'YXZ'`-order Euler that doesn't match `Object3D.rotation`'s own default `'XYZ'` decomposition, so reading `.rotation.y` directly would be quietly wrong once pitch is non-zero. **Rules out:** syncing full camera quaternion/orientation — more data than a placeholder capsule can even display.
2. **`player:move` is fire-and-forget, not acked, and rebroadcast as the same small delta (`socket.to(sessionId).emit`, excluding the sender) rather than folded into a `session:state` full-state broadcast.** Matches `docs/engineering/architecture.md`'s "Performance" section directly ("nicht bei jeder Änderung der komplette Game State übertragen") — this event fires up to ~10 times/second per moving player, so a full-`GameState` broadcast or an ack round-trip on every one would scale badly with player count. The server still validates and applies it (`SessionStore.move`, shape-checked in `validation.ts`) so a later joiner's initial `session:state` reflects everyone's current position — a malformed payload is silently dropped rather than acked with an error, since there's no ack channel to put an error on in the first place. **Rules out:** an ack-based request/response for movement, and reusing `session:state` for per-frame position updates.
3. **Client-side, avatar *membership* (who has a capsule at all) and *live movement* (where it currently is) are deliberately different code paths, not one.** `PlayerAvatars.sync(players, selfId)` runs only from `GameState.players` — infrequent, only on join/leave. `PlayerAvatars.updateOne(...)` runs from live `player:move` broadcasts — frequent. If a single path handled both, either movement updates would get needlessly reconciled against a stale `GameState.players` array on every frame, or a join/leave would require re-deriving live positions it doesn't have. Kept as two methods on the same class instead of two classes, since they share the mesh map. **Rules out:** re-running `sync()` on every animation frame.
4. **A new player's server-side default position/rotation is a real spawn point (`DEFAULT_SPAWN_POSITION`, shared between client and server), not the origin.** First tried `{x:0,y:0,z:0}` — harmless in Milestone 2 (nothing rendered a token there), but once avatars render from it, a freshly-joined player's own placeholder position defaults to underground-at-the-origin (`y - PLAYER_EYE_HEIGHT` math with `y=0` goes negative) until their first `player:move` corrects it. Moved the constant into `shared/src/types.ts` so `RoomLayout.ts`'s `PLAYER_EYE_HEIGHT` and `sessionStore.ts`'s `createPlayer` default derive from the same value rather than two literals that could drift. **Rules out:** leaving the mismatch as a "flashes briefly, self-corrects" cosmetic bug — cheap enough to fix at the root once noticed.

**Verified:** `server/src/playerMove.test.ts` — two real `socket.io-client`s, one moves, the other receives the broadcast live and the mover does *not* receive their own echo; a later joiner's `session:state` reflects an already-moved player's position; a malformed move doesn't crash the server. Break-round done on the exit-check test (removed the `socket.to(...).emit(...)` rebroadcast, confirmed the "sees it live" assertion times out and fails; restored, confirmed it passes). `client/src/three/PlayerAvatars.test.ts` (6 tests) covers create/update/remove/dispose in isolation — these construct real `THREE.Group`/`Mesh`/`Geometry` objects directly under Vitest's `node` environment; no WebGL/DOM needed for that. Full `sanity-check` (lint/format/build/test, 68 tests across all 3 workspaces) clean. **Real browser verification** (two Chrome tabs, via browser automation): both players join the same session, see each other in the roster, room renders without console errors. **Known gap, same as Milestone 3:** couldn't drive WASD movement itself through the automated browser to visually confirm a *moving* remote avatar end-to-end, because Chrome rejects the automation tool's synthetic clicks for Pointer Lock — confirmed directly this session (`document.pointerLockElement` stayed null after a synthetic click). Relied on the server integration test + client unit tests to cover what the browser check couldn't reach.

## 2026-09-23 — Milestone 3 part 2: real Blender room built and swapped in; lighting driven client-side, not baked into the glb

**Decided**, closing out Milestone 3.

1. **The room was modeled by driving Blender's Python API directly over MCP (`execute_blender_code`), not by hand in the Blender UI.** Floor, four walls, a round table (top + pedestal + base), a rug, a bookshelf with book blockouts, and a window were built as primitives with `bpy.ops.mesh.primitive_*`, matching `PLACEHOLDER_ROOM_LAYOUT`'s dimensions **exactly** (room bounds X -5..5 / Z -4..4, wall height 3m, table center (0,0) radius 1.1m, table height 0.75m). Exported via the MCP `export_scene`/`bpy.ops.export_scene.gltf` to `client/public/models/room.glb`, wired in through `RoomLoader.ts`'s existing `gltfUrl` seam (`RoomView.tsx` now passes `{ gltfUrl: '/models/room.glb' }`). Because the dimensions match the placeholder exactly, this is a pure asset swap — `client/src/three/collision.ts` and `RoomLayout.ts` needed zero changes, and the existing 13 collision unit tests remain valid proof without modification. **Rules out:** re-deriving room dimensions from the Blender export and back-porting them into `RoomLayout.ts` — matching first was cheaper and lower-risk.
2. **Lighting is set up directly in Three.js (`client/src/three/RoomLighting.ts`), not exported from Blender.** Initially tried `export_lights=True` on the glTF export (Blender's Watt-based point lights convert to `KHR_lights_punctual` candela via a fixed watts→lumens constant — a 600W Blender light became ~30,000 cd). Verified in a real browser (Chrome, via browser automation) that this overexposed Three's physically-correct renderer to a flat white floor with black voids elsewhere. Rather than hand-tune Blender wattages against an opaque unit conversion, lighting (a `HemisphereLight` + two `PointLight`s) is now added directly in Three's own photometric units after the room loads, alongside `renderer.toneMapping = ACESFilmicToneMapping` (previously unset, i.e. `NoToneMapping`, which was the other half of the overexposure — physically-correct light values need tone mapping to compress to displayable range). Tuned by iterating against real screenshots until material colors (wood floor, warm walls, dark table) read correctly instead of blown-out white. **Rules out:** baking lighting into future Blender-exported assets for this project — scene lighting is a client-side concern here, decoupled from whatever geometry/materials a `.glb` provides.
3. **The Blender source file is kept separately at `blender/room.blend` (repo root, not under any npm workspace), not inside `client/public/`.** `client/public/` is served as-is to the browser; the editable Blender source isn't a web asset and shouldn't ship. **Rules out:** losing the editable source after export, or accidentally serving it.
4. **Concurrent-agent gotcha, logged so it isn't rediscovered the hard way:** the Blender MCP add-on's socket server (`localhost:9876`) is a single shared endpoint — *any* session with the `blender` MCP tools loaded drives the same live Blender document, with no per-session isolation. Mid-session, a subagent (that should have been a no-op) kept running in the background after being told to stop, and built its own competing room in the same live scene concurrently with this session's work, producing ~82 conflicting objects (including furniture never asked for, e.g. an unexpected `FloorLamp_Shade`). Caught via `get_scene_info` showing an unrecognized object name and an object count far above what this session had created; resolved by stopping the errant agent (`TaskStop`) and wiping/rebuilding the scene clean. **Rules out:** assuming a live Blender MCP connection is exclusive to one session/agent — don't run a second agent against Blender concurrently, and if scene state ever looks larger or different than expected, check for exactly this before debugging the build script.
5. **Verified via real browser automation (Chrome extension), not just `sanity-check`.** Joined a session, confirmed the room (floor/walls/table/bookshelf/window, correct warm materials, no console errors, `room.glb` fetched with a 200) renders as intended after the lighting fix. **Known gap:** Chrome's Pointer Lock API rejects the automation tool's synthetic clicks (a browser security restriction, not a bug), so WASD movement itself couldn't be re-driven end-to-end this session — left to rest on the unchanged, already-unit-tested `collision.ts` (valid because the real room's dimensions match the placeholder's exactly, per #1).

**Verified:** full `sanity-check` (lint/format/build/test, all 3 workspaces, 46 tests) clean before and after. Manual `npm run dev` + real Chrome browser pass (see #5) — dev server processes confirmed stopped and ports 3001/5173 clear afterward.

## 2026-09-23 — Milestone 3 part 1 implementation: axis-separated collision, `PointerLockControls`, room/table sizing

**Decided**, four related calls implementing the movement engine (see the split decision below for why this is "part 1").

1. **Collision is resolved one axis at a time (try X, then Z), not as a single 2D vector clamp.** `client/src/three/collision.ts`'s `resolveMovement` tries the candidate X move, rejects it outright if it would overlap the table (keeping the old X), then does the same for Z. This is what makes sliding along a wall or around the table happen for free — the blocked axis freezes while the open one still carries the player. Plus a final unconditional "safety net" pass (re-clamp to room bounds, push out of the table if still overlapping) that corrects an already-invalid starting position rather than assuming the caller always starts valid. **Rules out:** a naive single clamped-vector approach, which would either fully block a diagonal move that's only partially obstructed, or require a more complex sliding-vector projection for the same result this gets for free from doing axes separately.
2. **`PointerLockControls` (Three.js's own addon) drives movement, not a hand-rolled mouse-look/WASD implementation.** `FirstPersonController` calls `controls.moveForward`/`moveRight` (which do the camera-relative direction math, including flattening out pitch), captures the resulting position delta, and re-resolves *that* through `resolveMovement` before committing it — rather than reimplementing forward/right vector math to get a delta before moving. **Rules out:** a custom camera-direction implementation, which would just be reinventing what the addon already does correctly.
3. **Room 10m × 8m, round table radius 1.1m (≈2.2m diameter), player collision radius 0.35m, eye height 1.7m, walk speed 3 m/s.** Sized for the "round table, 4-5 players" direction the user chose, with room to walk behind seated positions. Pure placeholder numbers — expected to be superseded by whatever the real Blender export measures (`docs/engineering/blender-workflow.md`), not a spec to preserve.
4. **Joining a session now shows the 3D room as the main view, with the session panel (code, player list, leave) as a small fixed overlay in the corner**, rather than replacing it or hiding it behind a toggle. Keeps Milestone 2's functionality reachable without a second UI mode. Small/reversible — flagged here rather than asked, per the "small layout calls are fine to make and document" guidance; a real visual-identity decision (the room's actual look) was asked and answered separately, see below.

**Verified:** 13 unit tests on `resolveMovement` (walls, table, corner clamping, sliding, already-invalid start positions). Two-stage break-round: disabling only the table-overlap check left the three "walk straight into the table" tests passing (the unconditional safety net catches it too — intentional defense-in-depth, not a test gap, since the caller-facing contract is "never end up overlapping" regardless of which internal check catches it) but correctly failed the sliding test, which depends on the axis-separated logic specifically; fully gutting `resolveMovement` to a raw passthrough then failed 11 of 13 tests, confirming the suite as a whole is not vacuous. Restored and reverified passing. Full `sanity-check` pass (lint/format/build/test, all 3 workspaces, 43 tests total) clean, both before the merge and reverified after.

## 2026-09-23 — Milestone 3 split in two: movement engine now, real Blender room next session

**Decided**, two related calls made kicking off Milestone 3.

**1. Blender MCP reused from `extraction_project`, not reinstalled.** Blender 5.2, the "MCP for Blender" add-on, and `uvx` were already installed on this machine for that repo (a user-global install, not per-repo). Added this repo's own `.mcp.json` registering the same `blender` server (`mcp-for-blender@2.0.0` via `uvx`) rather than reinstalling anything — per the standing instruction to check `extraction_project` for reusable setup first. Full detail: [engineering/blender-workflow.md](engineering/blender-workflow.md). **Rules out:** a fresh Blender/add-on install, which wasn't needed.

**2. M3 splits into "movement engine" (now) and "the real Blender room" (next session), because MCP servers only load at session startup.** The session that wrote `.mcp.json` doesn't have the `blender` tools yet — a fresh session is required before Blender can actually be driven over MCP. Rather than block all of M3 on that, the first-person camera/movement/collision system is built and tested now against a **procedural placeholder** (plain Three.js geometry, styled toward the "cozy tabletop game room" direction with color/lighting choices, but not the real asset), with the `GLTFLoader` integration path ready so swapping in the real Blender-exported room is a small change, not a rewrite. The actual Blender build (room + round table for 4-5 players, per the direction the user chose) happens in a subsequent session once the `blender` tools are available. **Rules out:** treating M3 as fully done once the procedural version passes its exit check — the roadmap entry stays open until the real Blender asset replaces the placeholder.

## 2026-09-23 — Milestone 2 design calls: implicit session creation, derived host role, disconnect ≠ leave, sessionStorage identity

**Decided**, four related calls made implementing Milestone 2 (`docs/roadmap.md`), none pinned down by the original spec:

1. **A session is created implicitly by its first joiner**, who becomes host. There's no `session:create` event — the spec's event list only has `session:join`/`session:leave`, and reading "join a code that doesn't exist yet" as "create it" avoids inventing an event the spec never named. **Rules out:** a separate creation flow/event for Milestone 2.
2. **Host/player role is derived, not stored per-player.** `GameState.hostId` already existed in the shared type; a player is host iff `player.id === state.hostId`. No new field on `Player`, no client-sent role claim possible — a client cannot fabricate host status because there's nothing to fabricate, only a comparison the server owns. **Host migration is explicitly out of scope**: if the host's player leaves, `hostId` keeps pointing at their now-absent id. Nothing crashes (no code dereferences `hostId` assuming a matching player exists), but there is currently no way for a session to get a new host once that happens. **Rules out:** building migration logic now — flagged for whichever milestone first needs host-gated actions to keep working after a host leaves (M5 `scene:*` is the first one that will need this).
3. **A raw socket disconnect does not remove the player from the session roster — only an explicit `session:leave` does.** This is what "a dropped client can rejoin the same session" (the M2 exit check) actually means: the `Player` record simply sits in `GameState.players` until either the same identity reconnects (`session:join` with the same `playerId` rebinds it to the new socket, no duplicate) or someone explicitly leaves. **Accepted downside:** a permanently-dropped client (closed tab, crashed browser) lingers in the roster forever, with no presence/timeout mechanism. **Rules out:** treating `disconnect` as an implicit leave, which would break reconnect entirely (the whole point of a dropped client rejoining is that their spot is still there).
4. **Client identity (`playerId`) is generated once and persisted in `sessionStorage`, not `localStorage`.** `localStorage` is shared across every tab of the same origin — two tabs of the same browser would collide onto one `playerId`, silently merging what should be two distinct players and breaking the "two browser tabs... see each other" exit check outright. `sessionStorage` is per-tab but still survives a same-tab reload, which is exactly the reconnect case this needs to support. **Rules out:** `localStorage` for this purpose, despite it being the more common default for "remember this across visits" — the per-tab requirement here overrides that.

**Verified:** `server/src/session.test.ts` connects two real `socket.io-client` instances against a real server and asserts each sees the other in the resulting player list, plus a disconnect/rejoin-same-identity/leave sequence. Break-round done on the core "Alice sees Bob join live" assertion (removed the room broadcast, confirmed the test times out and fails; restored, confirmed it passes). Also smoke-tested against the real `npm run dev` process pair (not just the in-process test) with two independent `socket.io-client` connections — functionally correct (both joins succeed, correct player lists in both acks), though the *broadcast* one of those manual clients happened to catch was the stale single-player self-broadcast rather than the later two-player one, because a one-shot `.once()` listener over a real network isn't guaranteed to catch a specific broadcast the way `waitForSessionState()` reliably does inside the same-process vitest test. Not a server defect: the real client (`App.tsx`) uses a persistent `.on()` listener, so it applies every broadcast in order and converges on the correct state regardless of which one a `.once()` happens to catch.

## 2026-09-23 — `.gitattributes` forces LF endings, overriding this machine's `core.autocrlf=true`

**Decided.** Right after the M1 merge, `npm run format:check` failed on all 27 tracked files. Cause: this machine's global git config has `core.autocrlf=true`, which rewrites every file to CRLF on checkout; Prettier (via `.prettierrc`, no `endOfLine` override) expects LF and flags every file as misformatted the moment git touches them. The git blobs themselves were already LF (git normalizes to LF on checkin regardless of `autocrlf`) — only the working tree was wrong, which is why the branch's own pre-merge checks passed: they ran before a later checkout re-triggered the CRLF conversion. Added `.gitattributes` (`* text=auto eol=lf`) so this repo's checkouts are LF regardless of the machine's global setting, ran `npm run format --write` once to fix the working tree, and reverified lint/format/build/test all clean. **Rules out:** relying on `core.autocrlf` (global, machine-specific, silently reintroduces this) instead of a repo-level `.gitattributes` (portable, travels with the repo). **Process note:** a sanity pass taken *before* a merge/checkout doesn't prove the *post-merge* working tree is clean on this OS — worth a quick reverify after any merge, not just before it.

## 2026-09-23 — TS project references dropped in favor of plain node_modules resolution for `shared`

**Decided.** `server/tsconfig.json` and `client/tsconfig.json` originally declared a TypeScript project reference (`"references": [{ "path": "../shared" }]`) to `shared`. `tsc` rejected it: a referenced project must be `"composite": true` and may not disable emit, which conflicts with `shared` being consumed as plain TS source (`main`/`types` pointing straight at `src/index.ts`, no build step — see [engineering/tooling.md](engineering/tooling.md)). Dropped the `references` array from both; `@custom-tabletop/shared` still resolves correctly for both type-checking and runtime through ordinary Node module resolution via the npm-workspace symlink in `node_modules`, which reads `shared/package.json`'s `main`/`types` fields directly — no project-reference machinery needed for a package with no build step of its own. **Rules out:** using `tsc -b` for this repo unless `shared` later gains a real build step (e.g. if it needs to ship compiled output to something that isn't `tsx`/Vite). **Verified:** `npm run build` type-checks clean in all three workspaces.

## 2026-09-23 — Server port 3001, client dev port 5173 (Vite default); ping/pong added as an infra-only event pair

**Decided.** Server listens on `3001` (`PORT` env var, default), Vite serves the client on its default `5173`; the client reads the server URL from `VITE_SERVER_URL` (see `client/.env.example`). None of the spec's domain events (`docs/engineering/architecture.md`) are a generic connectivity check, so a `connection:ping`/`connection:pong` pair was added in `shared/src/events.ts`, explicitly namespaced apart from `SocketEvent` (as `ConnectionEvent`) so it reads as infrastructure, not a Milestone-2+ domain event. **Rules out:** overloading a real domain event (e.g. `session:join`) as the M1 connectivity proof, which would've tied an infra concern to game logic prematurely.

## 2026-09-23 — `npm audit` vulnerabilities in dev-server tooling accepted, not fixed, for now

**Decided.** `npm install` surfaces 5 advisories (3 moderate, 1 high, 1 critical) in `vite`/`esbuild`/`vitest`'s dev-server request-handling code — all about a *local dev server* accepting requests it shouldn't, not a production runtime risk for a project with no Vite-dev-server-based deployment. Fixing means `npm audit fix --force`, which bumps to `vite@8`/`vitest@5` — a breaking-change upgrade out of scope for a toolchain-scaffolding milestone. **Rules out:** silently ignoring this going forward — logged here and in [engineering/tooling.md](engineering/tooling.md) so it gets revisited before anything is deployed or exposed beyond localhost, rather than forgotten.

## 2026-09-23 — Player experience is a walkable 3D room, not a 2D map with 3D accents

**Decided.** The source spec ([engineering/architecture.md](engineering/architecture.md)) describes a 2D-canvas-first VTT — a top-down map filling the screen, with a Three.js layer floating 3D dice/figures over it (Roll20/Foundry model). The actual product vision is a **Blender-built 3D room** (a big table, walls, interactable furniture) that the player **walks around in free first-person** (WASD + mouse-look, collision). The map/drawing surface becomes a `THREE.CanvasTexture` applied to the table inside that room, rather than a full-screen 2D canvas.

**Why this still fits the spec rather than replacing it:** the drawing event model, delta-only sync, `GameState` shape (`Player.position` was already `x/y/z`, not 2D), and server-authority rules are untouched — only *where* things render changes. The spec itself scopes Three.js for "future 3D tabletop objects," which a room and its furniture are simply more of.

**What's genuinely new, not implied by the original spec:** a Blender → glTF/GLB → `GLTFLoader` asset pipeline, first-person movement/collision code, and room interactables (their own event, e.g. `object:interact`, server-validated like everything else).

**Rules out:** the flat top-down map as the primary/only view, and an orbit-only camera (considered and rejected in favor of full walk-around — more scope, but it's the actual vision, not a simplification of it). **Reorders the roadmap:** room + first-person movement now land *before* the tabletop map/drawing milestone, since the room is the primary view the map lives inside of, not an overlay on top of it — see [roadmap.md](roadmap.md).

## 2026-09-23 — Repo workflow reused from `extraction_project`, trimmed to this project's scope

**Decided.** The docs/process structure (`CLAUDE.md` entry point, `docs/overview.md` + `decisions.md` + `changelog.md` as always-read logs, `docs/process/` for git workflow and session handover, `.claude/skills/` for `feature-workflow` and `sanity-check`) is carried over from the sibling `extraction_project` repo, which has iterated this pattern over many sessions. **Dropped:** the `parallel-agents` skill/doc and its worktree-based subagent orchestration — that repo is a large multi-system Unreal project where independent features genuinely parallelize; this project is small and solo right now. If scope grows enough to justify it, add it back modeled on that repo's version rather than improvising one. **Rules out:** treating this repo's process docs as needing the same weight as a mature project's — they should stay this light until the project's actual complexity demands more.

## 2026-09-23 — Documentation-only commits allowed straight to `main`; everything else needs a branch

**Decided**, matching `extraction_project`'s convention. **Why:** cheap for the common case (a doc fix shouldn't need branch ceremony) while still keeping `main` buildable — application code always goes through review-by-self on a branch first. **Rules out:** committing a docs change bundled with app code straight to `main`; if they're bundled, the whole commit goes through a branch.

## 2026-09-23 — Tech stack taken as given from the source specification, not re-litigated

**Decided.** The technical stack and architecture (Node.js + TypeScript backend, React + TypeScript frontend, Socket.IO, HTML5 Canvas for 2D, Three.js/WebGL for 3D, npm-workspace `client`/`server`/`shared` layout) came from a design specification handed to this session (preserved in [engineering/architecture.md](engineering/architecture.md)) and is treated as the starting point rather than something to evaluate alternatives for. The spec itself notes the concrete libraries may be adjusted during implementation as long as the functional requirements hold — so this isn't a hard lock, just a default not worth re-deciding before any code exists. **Rules out:** spending Milestone 1 time comparing frameworks (e.g. Vue vs. React) without a concrete reason one is failing.

---

## Table of Contents

**2026-09-24** — Milestone 5: tabletop map & drawing, draw-on-the-table interaction, runtime UV remap, single-scene scope cut, `Point2D` spec deviation

**2026-09-23** — Milestone 4: player avatars, yaw-only rotation, delta-only `player:move`, membership vs. live-movement split, shared spawn-position constant; Milestone 3 part 2: real Blender room built and swapped in, lighting driven client-side not baked into the glb, Blender MCP concurrent-session gotcha; Milestone 3 part 1: axis-separated collision, `PointerLockControls`, room/table sizing; Milestone 3 split (movement now, real Blender room next session), Blender MCP reused from `extraction_project`; Milestone 2: implicit session creation, derived host role, disconnect ≠ leave, sessionStorage identity; `.gitattributes` forces LF endings over this machine's `core.autocrlf`; TS project references dropped for `shared`; server/client ports and the infra-only ping/pong event pair; `npm audit` dev-tooling vulnerabilities accepted for now; player experience is a walkable 3D room, not a 2D map with 3D accents; repo workflow reused from `extraction_project`, trimmed to scope; documentation-only commits allowed to `main`; tech stack taken as given from the source spec
